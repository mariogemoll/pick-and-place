.DS_Store
.claude
.pnpm-store
.python-version
.vscode
node_modules
dist
build
pnpm-lock.yaml
*.log
/assets/apriltags
/dist_assets
/intermediary-glb
/intermediary-glb-generic
/config/camera_intrinsics/*.json
/config/camera_extrinsics/*.json
*.bak

# Working notes, deliberately uncommitted.
/docs/

# Machine-local data. New work writes under $PAP_DATA_ROOT instead; these are
# the pre-existing in-tree locations, kept ignored so old data does not surface
# in git status or get walked by repository-wide checks.
/datasets
/outputs/
/output/
/extras/
/out/
/py/out/
/.cache/
/py/.cache/
# wandb writes run directories under the process's cwd, which for these
# trainers is py/. Its blobs are far over the 40 KB file ceiling, and that
# check walks the whole history, so one committed run would be permanent.
/wandb/
/py/wandb/
